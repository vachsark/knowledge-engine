---
id: "{{id}}"
title: "{{title}}"
created: "{{created_at}}"
updated: "{{updated_at}}"
version: {{version}}
tags:
{{#each tags}}
  - {{this}}
{{/each}}
connections:
{{#each connections}}
  - {{this}}
{{/each}}
grounded_in:
{{#each grounded_in}}
  - {{this}}
{{/each}}
gate_score: {{gate_score}}
source_topic: "{{source_topic}}"
---

# {{title}}

<!-- One atomic concept. If you're covering more than one idea, split into multiple notes. -->

{{content}}

## Related

<!-- Links to connected notes. These should be bidirectional — update the target notes too. -->

{{#each connections}}

- [[{{this}}]]
  {{/each}}

---

_Generated by knowledge-engine v{{engine_version}} — {{created_at}}_
